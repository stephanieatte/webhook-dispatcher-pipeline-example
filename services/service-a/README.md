# service-a (example)

Placeholder for whatever service-a actually is. Its only relevant feature
for this example repo is `.buildkite/pipeline.yml`, which is triggered by
the root dispatcher pipeline whenever a change under this directory (or
`libs/shared-proto/`) is pushed.
